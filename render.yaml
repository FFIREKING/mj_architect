# Node Web Service — reliable SPA routing (no dashboard rewrite rules needed)
services:
  - type: web
    name: mj-architect
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
